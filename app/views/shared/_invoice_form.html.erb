<%= form_for(@invoice) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :oggetto, placeholder: "Oggetto della fattura" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>